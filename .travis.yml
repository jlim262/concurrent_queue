language: cpp
sudo: required

compiler:
  - gcc

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.8
      - g++-4.8
